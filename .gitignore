# Dataset (too large for git; download from Zenodo — see README)
data/

# Python
__pycache__/
*.pyc
.venv/

# Experiment artifacts (model checkpoints, MLflow runs)
*.pth
mlruns/

# OS
.DS_Store

# Claude Code local session files (shared rules in .claude/rules/ stay tracked)
.claude/settings.local.json
.claude/worktrees/

# Poster source archive (extracted copy is tracked in poster_overleaf/SMM_src/)
poster_overleaf/SMM.zip
